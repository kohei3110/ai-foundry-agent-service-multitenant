# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: ai-foundry-agent-service-multitenant
metadata:
  template: ai-foundry-agent-service-multitenant@0.0.1-beta

# Infrastructure location for deployment
infra:
  provider: bicep
  path: ./infra

# Pooled アーキテクチャ用のサービス定義
services:
  pooled:
    project: ./pooled
    host: containerapp
    language: csharp
